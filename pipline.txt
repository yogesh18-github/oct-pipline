pipeline {                     //declare always "pipeline" keyworld
    agent (any)                  //where to run the job (any-> job will run on any available executor )
    stages {
        stage('Stage-print-print-messgae') 
    {            steps {sh 'echo job-is-building'}
                
            
        }
		}